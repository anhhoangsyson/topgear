# Schema Validations — Zod Schemas

Vị trí: `src/schemaValidations/`

Mô tả
- Chứa các schema Zod dùng để validate form inputs và payloads (ví dụ: đăng nhập, đăng ký, product payload,...).

Lưu ý
- Sử dụng schema để validate cả client & server khi có thể, tránh duplication.
