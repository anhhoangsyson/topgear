# Hooks — Custom React Hooks

Vị trí: `src/hooks/`

Mô tả
- Chứa các hook tái sử dụng như `use-socket.ts`, `use-toast.ts`, `useUserInfo.ts`.

Lưu ý
- Hook dùng socket hoặc truy cập `window` phải được gọi trong component `'use client'`.
